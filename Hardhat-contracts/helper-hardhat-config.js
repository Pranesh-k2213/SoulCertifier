const networkConfig = {
  31337: {
    name: "localhost",
  },
};

const developmentChains = ["hardhat", "localhost"];
const verificationBlockConformation = 6;

module.exports = {
  networkConfig,
  developmentChains,
  verificationBlockConformation,
};
